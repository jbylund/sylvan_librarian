name: JS Tests

on:
  pull_request:
    paths-ignore:
      - '**/*.js'
      - 'api/static/fixtures/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/js-tests.yml'

permissions:
  contents: read

jobs:
  js-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No JS changes, skipping tests"
