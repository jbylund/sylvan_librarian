RESULT: the gated card-invariant rule is CORRECT and reaches 0.9% of queries. It does not narrow
the spread, and it does not explain compose's 20x.

IMPLEMENTED. `filter_touches_divergent_format(filter, planes.divergent_formats)` plus, in compose's
stream_scan_units chain, ahead of the legality arm:

    } else if composed_card_invariant && !filter_touches_divergent_format(..) { 0 }

`BitPlanes::divergent_formats` already existed and needed no archive change; its own doc states the
semantics the analysis had derived independently -- "a legality plane for a format outside this mask
is card-invariant, so a card-level bit implies every printing of that card".

REACH. stream_scan_units changed on 69 of 7,945 queries (0.9%), all on printing_compose, all to 0.
Of the 1,840 rows with residual_card_invariant true, only 69 changed: the rest already received 0
from the `tier == 0` arm, were not compose, or took the legality arm. Card-invariant AND tier > 0 is
a rare combination.

EFFECT.
  StreamedSelect pred/measured   p50 0.91 -> 0.90,  p10-p90 spread 3.8x -> 3.8x
  feature/realized pooled        0.13/1.00/2.71 -> 0.13/1.00/2.74
  picked_plan flips              1 of 7,944 (GatheredScan -> StreamedSelect)
  routing loss (picked - best)   2.49 ms -> 2.08 ms, but off ONE flip, so treat as noise
  cargo test 310 debug, clippy -D warnings clean

TWO CORRECTIONS THIS FORCES.

1. THE METRIC WAS BLIND. "How much narrower can we get the spread" cannot see this fix at all: on a
   card-invariant row the realized counter is 0, so the row is excluded from any feature/realized
   ratio by the divide-by-zero guard. Setting the feature to 0 where truth is 0 is invisible to that
   metric BY CONSTRUCTION. The spread being unchanged is not evidence the fix failed.

2. THE HYPOTHESIS WAS WRONG ANYWAY. The chain was "compose grades 20.0x while candidates grades
   2.7x, because compose's fix does not cover compose -- extend card-invariance to it". The extension
   reaches 0.9% of queries, so missing card-invariance is NOT what makes compose's rows dispersed.
   The dispersed compose rows have tier > 0 and are NOT card-invariant: genuinely printing-varying
   residuals where P3 does walk spans, and where stream_scan_units falls through to the big `else`
   branch -- Round 30's redo-pass calibration, fit as a wall-clock residual because no structural
   counter existed for the redo's real work. THAT is where compose's 20x lives, and it is the next
   place to look.

KEPT OR NOT is a judgment call: correct on 69 rows, no measurable win, no measurable harm, one flip.

FOLLOW-UP: ARE THE COUNTERS THERE, AND IS THE REDO DOUBLE-CHARGED?

Counters: YES. Round 31 added `PhaseStats::redo_examined`, "the exact printing count the redo pass
re-examines", and refit the else branch directly against it -- superseding Round 30's wall-clock
residual chain. A separate `REDO_SCAN_PER_ROW` term (`stream_redo_printings`) now consumes that
counter too.

Double-charge hypothesis: REFUTED. The else branch adds `2.237 * redo_candidates` into
stream_scan_units in CARD MODE ONLY, priced at STREAM_SCAN_PER_ROW_NS; REDO_SCAN_PER_ROW charges
`stream_redo_printings * STREAM_REDO_SCAN_PER_ROW_NS` gated on stream_runs_small_gather, and the two
rates are equal (both 5.97). If that were a double charge, card mode would be over-predicted
relative to printing/artwork on small-total rows. It is not:

  bucket                                          n     p10     p50     p90
  printing/artwork + large                    2,317    0.44    1.10    1.62
  card mode + large                           1,070    0.35    1.11    1.64
  printing/artwork + small-total               1,713    0.52    0.82    6.26
  card mode + small-total                       917    0.55    0.86    6.76

Card mode (which gets the addend) and printing/artwork (which does not) have nearly identical
profiles, and small-total rows are UNDER-predicted at the median -- the wrong direction for a
double charge.

WHAT THE SPLIT DOES SHOW, and it is the sharpest localization yet: ALL of StreamedSelect's
prediction dispersion is in the SMALL-TOTAL branch. p90 6.26-6.76 there against 1.62-1.64 on
large-total rows, in both modes. Large-total StreamedSelect is well predicted; the small-total redo
branch is not.

AND THAT BRANCH'S OWN TERM ALREADY DIAGNOSES ITSELF. `stream_redo_printings`' doc: it reads
p10 0.19 / p50 0.93 / p90 3.38 against `redo_examined`, and "the tail is the cardinality estimate
arriving through `matches` rather than this shape". Its per-card printing count is the CORPUS ratio
`n_printings / n_cards`, "because nothing on `PlanFeatures` describes the printing span of the
MATCHING subset specifically -- `scan_units` spans the candidates, not the matches."

So the missing quantity is named: the printing span of the MATCHING subset. That is a missing
FEATURE, consistent with the signature this whole item has shown (perfect median, wide dispersion,
no covariate), and it is not reachable by refitting either redo constant.
